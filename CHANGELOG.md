# Changelog

## [Unreleased]

### Changed

- Made `main` the lightweight `pi-cmux-lite` variant.
- Limited the package to `cmux-notify`, `cmux-split`, `cmux-open`, and `cmux-core`.
- Removed bundled prompts, skills, installer metadata, and legacy split aliases from the lite package.
- Updated package metadata and docs for the `travisp/pi-cmux-lite` fork.
